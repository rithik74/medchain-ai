{
  "name": "medchain-blockchain",
  "version": "1.0.0",
  "description": "MedChain AI Blockchain - Medical Record Hash Storage",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:amoy": "npx hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.19.4"
  }
}
